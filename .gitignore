# Dependencies & build output
node_modules/
dist/
dist-ssr/
*.local

# Environment & secrets
.env
.env.*
!legacy-vault/api/.env.example
!legacy-vault/ui/.env.example

# OS & editor
.DS_Store
.idea

# Daml build artifacts (keep ui/src/generated/ in git for Vercel deploy)
legacy-vault/.daml/

# Canton sandbox runtime logs
legacy-vault/log/

# Separate clones (maintained elsewhere)
linkup_mcp/
decentralized-will-management/
