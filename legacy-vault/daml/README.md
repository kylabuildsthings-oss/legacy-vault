# Daml contracts (Step 2)

Placeholder for `Vault.daml` and `VaultAdmin.daml`.

After installing the Daml SDK:

```bash
# From LEGACYVAULT root, if you need the full template:
daml new legacy-vault-daml --template create-daml-app
# Merge daml/ sources here, or run daml start from a merged project.
```

Contracts will encode party-scoped visibility: testator, heirs, oracle, trust administrator.
